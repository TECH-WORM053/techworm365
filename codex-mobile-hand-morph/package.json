{
  "name": "codex-mobile-hand-morph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-mediapipe-assets.mjs",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node scripts/sync-mediapipe-assets.mjs",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/app.js && node --check src/hand-tracker.js && node --check src/point-cloud.js"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
