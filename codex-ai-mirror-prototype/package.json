{
  "name": "codex-live-vision-mirror",
  "version": "0.3.0",
  "private": true,
  "description": "An on-device live person-tracking, reactive-background and catalog-ranking mirror prototype",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "start:lan": "node server.mjs --lan",
    "check": "node --check app.js && node --check catalog-data.js && node --check recommendation-engine.js && node --check vision-engine.js && node --check live-renderer.js && node --check server.mjs && node --check smoke-test.mjs",
    "test": "node smoke-test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
